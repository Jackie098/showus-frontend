import styled from 'styled-components';

export const StyledFooter = styled.footer`
  font-size: 14px;
  font-weight: bold;
  color: var(--text-color);
  text-align: center;
  padding-bottom: 35px;
`;
